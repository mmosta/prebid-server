{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "DMX Adapter Params",
  "description": "A schema which validates params accepted by DMX adapter",
  "type": "object",
  "properties": {
    "memberid": {
      "type": "integer",
      "description": "Required, an ID which identifies the publisher selling the impression"
    },
    "dmxid": {
      "type": "integer",
      "description": "Optional district m tag ID, required only for deal targeting"
    }
  },
  "required": [
    "memberid"
  ]
}
